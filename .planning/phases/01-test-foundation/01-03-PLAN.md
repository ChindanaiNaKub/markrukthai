---
phase: 01-test-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: true
requirements:
  - FOUND-01
gap_closure: true
must_haves:
  truths:
    - "npm run lint from project root runs ESLint across both workspaces"
    - "npm run lint:fix from project root fixes linting issues in both workspaces"
  artifacts:
    - path: "package.json"
      provides: "Root-level lint scripts for all workspaces"
      contains: '"lint":'
      contains: '"lint:fix":'
  key_links:
    - from: "package.json"
      to: "client/package.json, server/package.json"
      via: "npm workspace syntax"
      pattern: "--workspace="
---

<objective>
Add root-level lint scripts to enable running ESLint across all workspaces from project root

Purpose: User testing (01-UAT.md Test 1) revealed that `npm run lint` fails from the project root because root package.json lacks lint scripts. This is a planning gap from 01-01-PLAN.md which only specified scripts for client/ and server/ subdirectories.

Output: Root package.json with lint and lint:fix scripts that delegate to both workspaces
</objective>

<execution_context>
@/home/prab/.claude/get-shit-done/workflows/execute-plan.md
@/home/prab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-test-foundation/01-UAT.md
@.planning/phases/01-test-foundation/01-01-SUMMARY.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Existing root package.json scripts pattern (from current package.json): -->
From /home/prab/Documents/markrukthai-1/package.json:
```json
{
  "scripts": {
    "render-build": "npm install --include=dev && npm run build",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm run dev --workspace=server",
    "dev:client": "npm run dev --workspace=client",
    "build": "npm run build --workspace=client && npm run build --workspace=server",
    "validate:puzzles": "npm run validate:puzzles --workspace=server",
    "start": "npm run start --workspace=server"
  }
}
```

Pattern to follow: Use `npm run <script> --workspace=<name>` syntax for workspace delegation.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add lint scripts to root package.json</name>
  <files>package.json</files>
  <read_first>
    - /home/prab/Documents/markrukthai-1/package.json
  </read_first>
  <action>
    Add lint and lint:fix scripts to the root package.json scripts section.

    The scripts should follow the existing workspace delegation pattern used by other scripts (dev, build, etc.):

    Add to the "scripts" section (after "start", alphabetically):
    ```json
    "lint": "npm run lint --workspace=client && npm run lint --workspace=server",
    "lint:fix": "npm run lint:fix --workspace=client && npm run lint:fix --workspace=server"
    ```

    This ensures:
    - `npm run lint` from root runs ESLint on both client and server
    - `npm run lint:fix` from root fixes issues in both workspaces
    - The pattern matches existing scripts like "build" which uses workspace delegation

    Reference UAT gap diagnosis: "npm run lint from root fails - no lint script at root level"
  </action>
  <verify>
    <automated>grep -E '"lint"|"lint:fix"' /home/prab/Documents/markrukthai-1/package.json</automated>
  </verify>
  <done>
    - Root package.json contains "lint" script that runs both workspaces
    - Root package.json contains "lint:fix" script that fixes both workspaces
    - Scripts follow the workspace delegation pattern
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify root lint scripts work</name>
  <files>package.json</files>
  <read_first>
    - /home/prab/Documents/markrukthai-1/package.json
  </read_first>
  <action>
    Verify the new root-level lint scripts work correctly:

    1. Test lint from project root:
       ```bash
       npm run lint
       ```

    2. Test lint:fix from project root:
       ```bash
       npm run lint:fix
       ```

    Both commands should execute ESLint on client and server workspaces.
    Expected: Commands run successfully (exit code 0 or 1 for lint issues found, but not "missing script" error).

    This closes the UAT gap: "npm run lint from root fails - no lint script at root level"
  </action>
  <verify>
    <automated>npm run lint 2>&1; echo "Exit code: $?"</automated>
  </verify>
  <done>
    - `npm run lint` from root executes without "missing script" error
    - `npm run lint:fix` from root executes without "missing script" error
    - Both commands run ESLint across client and server workspaces
  </done>
</task>

</tasks>

<verification>
Overall verification:
1. Run `npm run lint` from project root - executes ESLint on both workspaces
2. Run `npm run lint:fix` from project root - fixes issues in both workspaces
3. Verify no "missing script" errors when running from root
</verification>

<success_criteria>
1. `npm run lint` works from project root
2. `npm run lint:fix` works from project root
3. UAT Test 1 gap is closed (user can run lint from root)
</success_criteria>

<output>
After completion, create `.planning/phases/01-test-foundation/01-03-SUMMARY.md`
</output>
